apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: buildwithruud-uat
type: Opaque
stringData:
  # 10.42.0.1 routes from K3s pods back to the Nix server's host network
  DATABASE_URL: "postgres://ruud_admin:<PLACEHOLDER>@10.42.0.1:5432/anchor_uat"
  RP_ORIGIN: "https://uat.buildwithruud.com"
  RP_ID: "uat.buildwithruud.com"
  LEPTOS_SITE_ADDR: "0.0.0.0:3000"
  LEPTOS_ENV: "production"
